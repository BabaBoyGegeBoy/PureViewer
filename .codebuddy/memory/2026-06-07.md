# 2026-06-07

## PureViewer 开发
- 创建开发文档 DEV.md
- 实现完整图片查看器功能：文件夹选择、大图展示、GIF动画播放、导航、全屏、键盘快捷键
- 修复 UseWindowsForms 导致的命名空间歧义（添加 Using Remove）
- 添加子文件夹遍历功能（含子文件夹 CheckBox）
- 功能扩充（全部完成，编译通过 0 错误 0 警告）：
  1. 鼠标滚轮浏览
  2. 按钮悬停高亮（全局 Button Style）
  3. 图片加载状态反馈（加载中覆盖层）
  4. 按作者筛选（从目录名 `作者名_ID` 提取）
  5. 按文件名/标题搜索（带 placeholder 搜索框）
  6. 作者列表侧栏（ListView 180px 左侧栏）
  7. 按标签筛选（解析 info.json tags 数组）
  8. 图片缩放+拖拽平移（Ctrl+滚轮/快捷键缩放，放大后拖拽）
  9. 网格缩略图视图（G 键切换，WrapPanel 布局，120px 缩略图）
- 修复 Point 命名空间冲突（使用 System.Windows.Point 全限定名）
- UI 重构方案A→方案B：28px极简顶栏始终可见，底栏hover展开+3秒自动折叠动画，Tab切换侧栏
